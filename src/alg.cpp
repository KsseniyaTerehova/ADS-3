// Copyright 2021 NNTU-CS

int cbinsearch(int *arr, int size, int value) {
  if (arr[mid] == value) {
    for (int i = mid; i < size && arr[i] == value; ++i)
      count++;
    break;
  }
  return 0;
}
